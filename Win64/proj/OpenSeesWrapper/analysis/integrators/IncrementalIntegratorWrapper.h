#pragma once
#include "IntegratorWrapper.h"

namespace OpenSees {

		public ref class IncrementalIntegratorWrapper abstract : IntegratorWrapper
		{
		public:
			IncrementalIntegratorWrapper();
			~IncrementalIntegratorWrapper();
		internal:

		private:

		};
	}
