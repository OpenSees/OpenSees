c.... *****************************************************************<-70
c      this files contains some dummy feap commands that are never called
c      by an fElement but that are required in the feap element
c.... *****************************************************************<-70

      subroutine dinput(d,i)
      implicit none
      real*8 d(*)
      integer i

      end
